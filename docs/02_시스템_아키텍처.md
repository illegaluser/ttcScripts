# DSCORE-TTC 시스템 아키텍처

## 1. 배포 구조: 단일 머신, 폐쇄망 대응

DSCORE-TTC의 모든 서비스는 **개발자의 로컬 머신(맥북) 한 대** 위에서 구동됩니다. 외부 서버, 클라우드 인프라, 인터넷 연결이 필요하지 않습니다.

```text
┌─────────────────────────────────────────────────────────────────────┐
│                   개발자 맥북 (단일 로컬 머신)                        │
│                                                                     │
│   ┌───────────────────────────────────────────────────────────┐     │
│   │              Docker Compose (devops-net)                   │     │
│   │                                                           │     │
│   │   Jenkins    SonarQube    GitLab    Langfuse    DB x2     │     │
│   │   :8080      :9000        :8929     :3000                 │     │
│   └───────────────────────────────────────────────────────────┘     │
│                          │                                          │
│   ┌────────────────┐     │     ┌──────────────────────────────┐     │
│   │  Dify          │◄────┘     │  Ollama (로컬 LLM 엔진)      │     │
│   │  (별도 Compose) │           │  :11434                      │     │
│   │  AI 워크플로우  │           │  qwen3-coder, llama3.2-vision │     │
│   └────────────────┘           └──────────────────────────────┘     │
│                                                                     │
│   ※ 외부 인터넷 연결 불필요. 모든 데이터가 이 머신 안에서 처리됩니다.  │
└─────────────────────────────────────────────────────────────────────┘
```

### 폐쇄망에서의 동작 원리

| 구성 요소 | 외부 연결 필요 여부 | 설명 |
| --- | --- | --- |
| Jenkins, SonarQube, GitLab | 불필요 | Docker 이미지를 사전에 다운로드하여 로컬에 보관합니다. 이후 인터넷 없이 구동 가능합니다 |
| Dify | 불필요 | 자체 Docker Compose로 로컬 구동합니다 |
| Ollama (LLM) | 최초 모델 다운로드 시에만 필요 | 모델을 한 번 받으면 이후 인터넷 없이 무제한 사용 가능합니다. 추론 비용은 0원입니다 |
| Langfuse | 불필요 | 로컬 PostgreSQL에 데이터를 저장합니다 |
| 외부 AI (OpenAI, Gemini) | **선택 사항** | 개방망 환경에서 성능을 높이고 싶을 때만 사용합니다. 폐쇄망에서는 완전히 생략 가능합니다 |

### 개방망 vs 폐쇄망 구성 비교

| 항목 | 개방망 (인터넷 가능) | 폐쇄망 (인터넷 차단) |
| --- | --- | --- |
| LLM 추론 | 로컬 Ollama + 외부 AI(OpenAI/Gemini) 병행 가능 | 로컬 Ollama만 사용 |
| AI 평가 심판 | 로컬 모델 또는 GPT-4o 등 고성능 외부 모델 선택 가능 | 로컬 모델(qwen3-coder 등)만 사용 |
| Docker 이미지 | 빌드 시 자동 다운로드 | 사전에 이미지를 `docker save/load`로 반입해야 합니다 |
| Ollama 모델 | `ollama pull`로 즉시 다운로드 | 사전에 모델 파일을 USB 등으로 반입해야 합니다 |

---

## 2. 서비스 상세 구성도

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          사용자 접점                                     │
│                                                                         │
│   Jenkins UI (:8080)     Dify UI            GitLab UI (:8929)           │
│   파이프라인 실행/관리    워크플로우 설계      소스 코드/이슈 관리          │
│        │                     │                     │                    │
└────────┼─────────────────────┼─────────────────────┼────────────────────┘
         │                     │                     │
         ▼                     ▼                     ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    Docker 네트워크 (devops-net)                           │
│                                                                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌───────────┐  ┌─────────┐ │
│  │ Jenkins   │  │ SonarQube│  │ GitLab   │  │ Langfuse  │  │ Dify    │ │
│  │ :8080     │  │ :9000    │  │ :8929    │  │ :3000     │  │ (별도)  │ │
│  │           │  │          │  │          │  │           │  │         │ │
│  │ 파이프라인 │  │ 정적분석  │  │ Git+이슈 │  │ AI 관측   │  │ LLM+KB │ │
│  └─────┬─────┘  └────┬─────┘  └────┬─────┘  └─────┬─────┘  └────┬────┘ │
│        │             │             │              │             │      │
│        └─────────────┴─────────────┴──────────────┴─────────────┘      │
│                                    │                                    │
│                         host.docker.internal                            │
│                                    │                                    │
└────────────────────────────────────┼────────────────────────────────────┘
                                     │
                              ┌──────┴──────┐
                              │   Ollama     │
                              │ :11434       │
                              │ 로컬 LLM     │
                              │ (호스트 실행) │
                              └─────────────┘
```

---

### 서비스별 역할

| 서비스 | 포트 | 역할 | 데이터 저장 위치 |
| --- | --- | --- | --- |
| **Jenkins** | 8080 | 8개 파이프라인의 실행 엔진. 모든 자동화 작업의 시작점 | `data/jenkins/` |
| **SonarQube** | 9000 | 소스 코드 정적 분석. 버그, 취약점, 코드 스멜을 탐지 | `data/sonarqube/` |
| **GitLab** | 8929 | Git 저장소 호스팅 + 이슈 트래커. Job #4가 이슈를 자동 생성하는 대상 | `data/gitlab/` |
| **Dify** | 80 | AI 워크플로우 설계 플랫폼. Knowledge Base(RAG)와 Chatflow를 제공 | `dify/docker/volumes/` |
| **Langfuse** | 3000 | LLM 호출의 관측성 대시보드. 평가 점수, 지연 시간, 토큰 사용량 추적 | `data/postgres-langfuse/` |
| **Ollama** | 11434 | 로컬 LLM 실행 엔진. 호스트 머신에서 직접 구동. 인터넷 없이 추론 수행 | 호스트 `~/.ollama/` |

---

## 3. 데이터 흐름

### Phase 2: 지식 관리 흐름

```
[원본 문서]                    [Jenkins Job #1~#3, #5]              [Dify Knowledge Base]
PDF, DOCX, XLSX  ─────────▶  Python 스크립트가 변환  ─────────▶  LLM이 검색 가능한
코드, 웹                       (Markdown/텍스트)                  벡터 저장소로 보관
```

### Phase 3: 품질 분석 흐름

```
[SonarQube]         [Jenkins Job #4]          [Dify Workflow]       [GitLab]
정적 분석 결과  ──▶  이슈 추출 (API) ──▶  LLM이 원인 진단  ──▶  Issue 자동 생성
(버그/취약점)                               + 수정 방안 제안        (중복 방지 포함)
```

### Phase 4: 자율 E2E 테스트 흐름

```
[사용자 입력]           [Dify Brain]              [Python Executor]         [산출물]
자연어 / 기획서  ──▶  9대 DSL JSON 생성  ──▶  브라우저 자동 실행  ──▶  scenario.json
/ 브라우저 녹화         (Planner LLM)          + Self-Healing            스크린샷, 로그
```

### Phase 5: AI 에이전트 평가 흐름

```
[시험지 CSV]       [Jenkins Job #7]           [DeepEval + Ollama]       [Langfuse]
golden.csv  ──▶  대상 AI에 질문 전송  ──▶  11대 지표 채점  ──▶  점수 대시보드
(질문+기대답변)     (API 또는 웹 UI)          (심판 LLM)              + Jenkins 아티팩트
```

---

## 4. 네트워크 구성 (devops-net)

모든 컨테이너 서비스는 `devops-net`이라는 Docker 가상 네트워크로 연결된다. 이를 통해 서비스 간에 컨테이너 이름(예: `sonarqube`, `gitlab`)으로 직접 통신할 수 있다.

| 통신 경로 | 사용되는 URL | 설명 |
| --- | --- | --- |
| Jenkins → SonarQube | `http://sonarqube:9000` | Job #4에서 정적 분석 결과를 API로 조회 |
| Jenkins → GitLab | `http://gitlab:8929` | Job #4에서 이슈를 API로 생성 |
| Jenkins → Dify | `http://api:5001/v1` | Job #1~#5에서 지식 업로드 및 워크플로우 실행 |
| Jenkins → Langfuse | `http://langfuse-server:3000` | Job #7에서 평가 결과를 전송 |
| Jenkins → Ollama | `http://host.docker.internal:11434` | 로컬 LLM 호출 (호스트 머신 경유) |

Dify는 별도의 Docker Compose로 관리되며, `docker-compose.override.yaml`을 통해 `devops-net`에 연결됩니다.

---

## 5. 보안 및 데이터 주권

### 데이터가 머신 밖으로 나가지 않습니다

DSCORE-TTC에서 처리되는 모든 데이터의 흐름은 다음과 같습니다.

| 데이터 종류 | 저장 위치 | 외부 전송 여부 |
| --- | --- | --- |
| 프로젝트 문서 (PDF, Word 등) | `data/knowledges/docs/` | 전송하지 않음 |
| 소스 코드 | GitLab 컨테이너 (`data/gitlab/`) | 전송하지 않음 |
| 정적 분석 결과 | SonarQube 컨테이너 (`data/sonarqube/`) | 전송하지 않음 |
| AI 학습 데이터 (Knowledge Base) | Dify 컨테이너 내부 볼륨 | 전송하지 않음 |
| AI 평가 결과 | Langfuse DB (`data/postgres-langfuse/`) | 전송하지 않음 |
| LLM 추론 (질문/답변) | Ollama (호스트 메모리) | 전송하지 않음 |
| E2E 테스트 스크린샷 | Jenkins 아티팩트 (`artifacts/`) | 전송하지 않음 |

> 외부 AI 서비스(OpenAI, Gemini 등)를 **선택적으로** 연동한 경우에만, 해당 서비스에 질문 데이터가 전송됩니다. 폐쇄망 환경에서는 이 연동을 사용하지 않으므로 데이터 유출 경로가 존재하지 않습니다.

### 폐쇄망 도입 시 사전 반입 체크리스트

인터넷이 차단된 환경에서 시스템을 구축하려면, 다음 항목을 사전에 반입해야 합니다.

| 반입 항목 | 용량 (참고) | 반입 방법 |
| --- | --- | --- |
| Docker 이미지 6종 (Jenkins, SonarQube, GitLab, Langfuse, PostgreSQL x2) | 약 10~15GB | `docker save` → USB → `docker load` |
| Dify Docker 이미지 | 약 3~5GB | 동일 |
| Ollama 설치 파일 (macOS) | 약 100MB | USB |
| LLM 모델 파일 (qwen3-coder:30b, llama3.2-vision 등) | 모델당 5~20GB | USB → `ollama` 모델 디렉터리에 복사 |
| 프로젝트 소스 코드 (본 리포지토리) | 약 500MB | USB → Git clone 또는 직접 복사 |
